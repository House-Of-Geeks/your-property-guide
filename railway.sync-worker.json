{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate"
  },
  "deploy": {
    "startCommand": "node -e 'console.log(\"sync-worker ready — trigger via Railway dashboard, ssh, or schedule\"); setInterval(()=>{}, 1<<30)'",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
