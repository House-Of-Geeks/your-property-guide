{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksVersion": "1.30.1"
  },
  "deploy": {
    "startCommand": "echo 'sync-worker idle; trigger via railway run npm run sync <source-id>'",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
