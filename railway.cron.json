{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.sync-cron"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
